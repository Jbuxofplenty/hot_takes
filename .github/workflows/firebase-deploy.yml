name: Deploy to Firebase

on:
  # Called by other workflows
  workflow_call:
    inputs:
      target:
        description: 'Firebase target (functions, firestore, hosting, all)'
        required: false
        default: 'all'
        type: string
    secrets:
      FIREBASE_TOKEN:
        required: true
  
  # Allow manual triggering from GitHub UI
  workflow_dispatch:
    inputs:
      target:
        description: 'Firebase target (functions, firestore, hosting, all)'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - functions
          - firestore
          - hosting

jobs:
  deploy:
    name: Deploy to Firebase
    runs-on: ubuntu-latest
    
    steps:
      - name: Check trigger source
        run: |
          if [ "${{ github.event_name }}" = "workflow_call" ]; then
            echo "🚀 Triggered by another workflow"
          else
            echo "🚀 Manually triggered deployment"
          fi
      
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'yarn'
          cache-dependency-path: |
            yarn.lock
            functions/yarn.lock
      
      - name: Install root dependencies
        run: yarn install --frozen-lockfile
      
      - name: Install Functions dependencies
        run: cd functions && yarn install --frozen-lockfile
      
      - name: Run Functions tests
        run: cd functions && yarn test:ci
        continue-on-error: false
      
      - name: Install Firebase CLI
        run: npm install -g firebase-tools
      
      - name: Deploy to Firebase
        run: |
          TARGET="${{ github.event.inputs.target || 'all' }}"
          PROJECT="${{ vars.FIREBASE_PROJECT_ID || 'default' }}"
          
          if [ "$TARGET" = "all" ]; then
            firebase deploy --project "$PROJECT" --non-interactive
          else
            firebase deploy --only $TARGET --project "$PROJECT" --non-interactive
          fi
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
      
      - name: Deploy Summary
        run: |
          echo "✅ Firebase deployment completed successfully!"
          echo "🔗 Project: ${{ vars.FIREBASE_PROJECT_ID || 'default' }}"
          echo "📦 Deployed: ${{ github.event.inputs.target || 'all' }}"
          echo "🌿 Branch: ${{ github.ref_name }}"
          echo "📝 Commit: ${{ github.sha }}"
